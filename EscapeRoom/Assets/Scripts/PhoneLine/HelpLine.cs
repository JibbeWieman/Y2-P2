using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelpLine : MonoBehaviour
{
    public enum STATES {
        INTRO,
        MAIN_PUZZLE_AREA, // The area that goes into find laptop password, solve binary book puzzles, and find QR code
        DECRYPT_PASSWORD // At the end when 
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
